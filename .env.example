# Aether OS - Environment Configuration
# Copy this file to .env and fill in your API keys.

# ── LLM Provider API Keys ──────────────────────────────────────
# At least one API key is needed for agents to use LLM reasoning.
# Without any key, agents fall back to heuristic demo mode.

# Google Gemini (default provider)
GEMINI_API_KEY=

# OpenAI (gpt-4o, gpt-4o-mini, gpt-3.5-turbo)
OPENAI_API_KEY=

# Anthropic (claude-sonnet-4-5-20250929, claude-haiku-4-5-20251001)
ANTHROPIC_API_KEY=

# Ollama (local models, no API key needed)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3.1

# ── Server Configuration ───────────────────────────────────────

# Port for the Aether OS server (default: 3001)
AETHER_PORT=3001

# Filesystem root for agent data (default: /tmp/aether)
AETHER_FS_ROOT=/tmp/aether

# ── Cluster Configuration ──────────────────────────────────────

# Set to 'hub' to run as cluster hub, 'node' to join a hub
# AETHER_CLUSTER_ROLE=standalone
# AETHER_HUB_URL=ws://hub-host:3001/cluster
