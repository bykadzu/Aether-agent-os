{
  "name": "@aether/embed",
  "version": "0.4.0",
  "type": "module",
  "main": "dist/aether-embed.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=iife --outfile=dist/aether-embed.js",
    "test": "vitest run"
  },
  "devDependencies": {}
}
